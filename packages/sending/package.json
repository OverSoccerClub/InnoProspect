{
  "name": "@inno/sending",
  "private": true,
  "version": "0.0.0",
  "description": "A sequência protegida de envio de mensagem (opt-out → guard → write-ahead → sendText → contabilidade), extraída de apps/web/src/lib/services/messages.ts para ser compartilhada com apps/worker (motor de disparo, Fase 4.F). Ver ARQUITETURA.md §6.8.0.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --config ../../eslint.config.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@inno/core": "workspace:^",
    "@inno/db": "workspace:^",
    "@inno/messaging": "workspace:^"
  }
}
