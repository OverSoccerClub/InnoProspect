// Placeholder — Vega implementa aqui o cliente Evolution API tipado (ver ARQUITETURA.md §4.8):
// evolution-client.ts (createInstance, qr, sendText, status), webhook-parser.ts,
// types.ts, errors.ts (classifica: retryable / ban / desconectado).
export {};
