// Fonte da verdade: @inno/contracts (`packages/contracts/src/dashboard.contract.ts`),
// publicado pelo Vega em paralelo a esta rodada — o formato batia campo a
// campo com o tipo local que eu tinha escrito antes de checar (ver
// convention-check-contracts-before-mocking na minha memória), então troquei
// pra reexportar em vez de manter duplicado.
export type { DashboardSummary } from '@inno/contracts';
