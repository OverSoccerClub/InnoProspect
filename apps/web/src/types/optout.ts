// Fonte da verdade: @inno/contracts (ARQUITETURA.md §4.7), publicado pelo
// Vega em paralelo a esta entrega. Reexporta com os nomes já usados em
// lib/api/optouts.ts, lib/api/public-optout.ts, mocks/* e componentes.
export type {
  OptOutSource,
  OptOutItem,
  CreateOptOutResponse,
  CreateOptOutBody as CreateOptOutRequest,
  PublicOptOutBody as PublicOptOutRequest,
  PublicOptOutResponse,
} from '@inno/contracts';
