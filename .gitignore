# --- Segredos (NUNCA versionar) ---
.env
.env.*
!.env.example

# --- Dependências ---
node_modules/
.pnpm-store/

# --- Build ---
.next/
out/
dist/
build/
*.tsbuildinfo
.turbo/

# --- Prisma ---
packages/db/src/generated/

# --- Build de @inno/db (tsc -p tsconfig.build.json, ver package.json#exports) ---
# index.ts/client.ts compilam EM LUGAR (ao lado do .ts) para o Node em
# produção conseguir importar @inno/db sem bundler — artefato, não fonte.
packages/db/src/index.js
packages/db/src/index.js.map
packages/db/src/client.js
packages/db/src/client.js.map

# --- Testes ---
coverage/
playwright-report/
test-results/

# --- Runtime / logs ---
*.log
npm-debug.log*
pnpm-debug.log*

# --- SO / editor ---
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# Missão aberta no momento, nesta máquina/sessão (Painel de Tarefas da
# InnovareCode, equipe.innovarecode.com.br). Estado transitório escrito por
# scripts/missao.mjs do repositório innovarecode-missoes — nunca versionar:
# cada pessoa/sessão tem o seu, e commitar faria um sobrescrever a tarefa em
# aberto do outro a cada pull.
.missao-atual

# Registro local do quadro de missoes (quadro.mjs) - estado de sessao, nunca versionar.
.missoes-quadro.json

# Configuracao local do preview do Claude Code: aponta para caminhos desta maquina.
.claude/launch.json
